#!/bin/bash

# Проверяем, передан ли аргумент
if [ $# -eq 0 ]; then
    echo "Ошибка: нужно указать команду для выполнения."
    exit 1
fi

# Запускаем указанную команду в фоне и сохраняем её PID
"$@" &
PID=$!

# Выводим PID запущенного процесса
echo "Команда '$*' запущена в фоне с PID: $PID"